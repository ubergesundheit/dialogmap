-# %contribution{ ng_repeat: "contribution in contributions", contribution: "contribution", :"data-contribution-id" => "{{contribution.id}}" }
.contributions-list{ ng_repeat: "contribution in Contribution.parent_contributions"}
  .contribution.contribution-arrow
    .contribution-info
      {{ contribution.user.email }}
      schrieb am
      {{ contribution.createdAt | date: "dd.MM.yy 'um' HH:mm:ss"}}
    %h3 {{ contribution.title }}
    .contribution-description{ ng_bind_html: "contribution.description" }
    %a.contribution-links{ :"ui-sref" => "contribution({ id: contribution.id })" }
      Antworten anzeigen
