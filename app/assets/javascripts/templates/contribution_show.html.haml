.contribution
  .contribution-info
    parent of:
    {{ contribution.id }}
    is
    {{ contribution.parentId || "keiner" }}
    |
    {{ contribution.user.email }}
    schrieb am
    {{ contribution.createdAt | date: "dd.MM.yy 'um' HH:mm:ss"}}
  %h3 {{ contribution.title }}
  .contribution-description{ ng_bind_html: "contribution.description" }
  .contribution_input_replace
    %button.contribution-replybutton{ ng_click: "startContributionHere(contribution.id)", ng_hide: "Contribution.composing" } Antwort verfassen
    -# .composing_container
    -# .contribution-list{ contributions_display: true, ng_model: "contribution.childrenContributions" }
    -# .contribution-replies{ ng_repeat: "reply in contribution.childrenContributions" }
      {{ reply }}
